node_modules/
playground/node_modules/
playground/dist/
*.local
.DS_Store
.yarn/
.playwright-mcp/
*.png
docs/.vitepress/cache/
docs/.vitepress/dist/
# Build output — publicado via npm (files:["dist"] + prepublishOnly), não versionado
dist/
# Grafo gerado pelo graphify
graphify-out/
# Config/tooling local do Claude Code (settings de máquina + skill vendorada)
.claude/settings.json
.claude/settings.local.json
.claude/skills/
